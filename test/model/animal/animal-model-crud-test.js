const shelterModelCRUDTest = (assert, Animal, ShelterAdmin, Shelter, seeds) => {
	describe('Animal CRUD model tests....', () => {
		
		it('uses ShelterAdmin#addANimal to add an animal to a Shelter', (done) => {
			assert( true === true);
			done()
		});	
	})
}

module.exports = shelterModelCRUDTest;